[package]
name = "proto"
version = "0.0.0"
edition = "2021"
publish = false

[lib]
name = "proto"
path = "src/lib.rs"

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
url = { workspace = true, features = ["serde"] }
